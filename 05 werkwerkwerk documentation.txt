VFO entry is passed to werkwerkwerk, and global variables are read.


#Do I prefer this happen in the scanNplan phase??
If VFO = valid:
    Assess if image is to be grown, shrunk, or left alone: G/S/L
    Assess if image need canvas: T/F
    Assess if PNG file is to be preserved: modify options
    Assess if image format is to be changed to JPG: T/F
        Assess if a new file has been created: T/F; Delete origin file: T/F?

Execute the above as a single command line, (possibly two if deleting origin file.)
